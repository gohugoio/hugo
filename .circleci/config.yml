defaults: &defaults
  working_directory: /go/src/github.com/gohugoio
  docker:
      - image: bepsays/ci-goreleaser
    
version: 2
jobs:
  build:
    <<: *defaults
    steps:
      - checkout:
          path: hugo
      - run:
            command: |
                git clone git@github.com:gohugoio/hugoDocs.git
                cd hugo
                make vendor
      - persist_to_workspace:
          root: .
          paths: .
  release:
    <<: *defaults
    steps:
      - attach_workspace:
          at: /go/src/github.com/gohugoio
      - add_ssh_keys:
          fingerprints:
            - "38:9e:19:58:e6:cf:ab:58:7d:92:61:b3:52:49:a3:e8"
      - run:
            command: |
                    cd hugo
                    go run -tags release main.go release -r ${CIRCLE_BRANCH}

workflows:
  version: 2
  release:
      jobs:  
        - build:
            filters:
              branches:
                only: /release-.*/
        - hold:
            type: approval
            requires:
              - build
        - release:
            context: org-global
            requires:
              - hold
